name: formdator_demo
description: How to use the Formadator package effectively.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0
homepage: https://dartoos.dev
repository: https://github.com/dartoos-dev/formdator

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  formdator:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0

flutter:
  uses-material-design: true
